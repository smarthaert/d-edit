Unit Help;

Interface

Uses TPCRT,TPWindow;

{ ******************** }
{ **** Подсказки ***** }
{ ******************** }

 Procedure helping;

Implementation

 Procedure helping;
  Var Window : WindowPtr;
   Begin

    MakeWindow(Window,2,5,78,24,True,True,False,$07,$07,$0F,'Помощь');
    DisplayWindow(Window);
    writeln('Программа "Полином" предназначена для работы с многочленами. Раздел "Полином" состоит из следующих подменю:');
    write('I) "Ввод нового". При его выборе нужно задать имя(не более 17 символов) и содержание полинома. Добавление члена ');
    writeln('происходит при нажатии ENTER на выделенном "Степень/Коэффициент".Редактирование данных производится аналогично.');
    writeln('Степень не должна быть дробной, больше 65535 и меньше 0. Коэффициент - не длиннее 53 символов.');
    writeln('II) "Вывод". Перед выводом нужно ввести имя нужного полинома.');
    writeln('III) "Удаление". Перед удалением нужно ввести имя нужного полинома.');
    writeln('IV) "Удаление всех". Удаление всего списка полиномов.');
    writeln('V) "Редактирование". Изменение степени и коэффициента у любого члена. Нужно нажать ENTER на выделенном члене.');
    writeln('Раздел "Действия" состоит из следующих подменю:');
    writeln('I,II) "Сложение" и "Вычитание" 2-х полиномов. Вводятся их имена и название полинома-суммы.');
    writeln('III) Сортировка". Применяется, если многочлен был введен неупорядоченно относительно степени.');
    write('Для выхода нажмите');
    readln;
    KillWindow(Window);
    End;

  End.